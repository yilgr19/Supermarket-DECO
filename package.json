{
  "name": "supermarket",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"run-api.cmd spring-boot:run\" \"wait-on http-get://127.0.0.1:8088/api/v1/health --timeout 120000 && npm.cmd run dev --prefix pos-frontend\"",
    "dev:api": "run-api.cmd spring-boot:run",
    "dev:web": "npm.cmd run dev --prefix pos-frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.3"
  }
}
